# Core dependencies for resume-tailoring-system
anthropic>=0.40.0
aiofiles>=24.1.0
pydantic>=2.0.0
pyyaml>=6.0.0
rich>=13.0.0
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Development dependencies (optional, install with: pip install -r requirements.txt -r requirements-dev.txt)
# Or use: pip install -e ".[dev]"
